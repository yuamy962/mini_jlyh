page {
  background-color: #f5f6fa;
}

.container {
  padding: 0 32rpx 48rpx;
}

/* 顶部区域 */
.header {
  padding: 56rpx 0 48rpx;
  text-align: center;
}

.main-title {
  font-size: 48rpx;
  font-weight: 800;
  color: #1a1a2e;
  margin-bottom: 16rpx;
  line-height: 1.4;
  letter-spacing: 2rpx;
}

.main-title .highlight {
  color: #2563eb;
}

.sub-title {
  font-size: 26rpx;
  color: #9ca3af;
  letter-spacing: 4rpx;
}

/* 表单卡片 */
.form-card {
  background: #fff;
  border-radius: 24rpx;
  padding: 48rpx 36rpx;
  box-shadow: 0 8rpx 40rpx rgba(0, 0, 0, 0.08);
}

.form-item {
  margin-bottom: 36rpx;
}

.form-item:last-of-type {
  margin-bottom: 0;
}

.label {
  font-size: 28rpx;
  font-weight: 600;
  color: #374151;
  margin-bottom: 16rpx;
}

.required {
  color: #ef4444;
}

.input {
  height: 96rpx;
  background: #fff;
  border: 2rpx solid #e5e7eb;
  border-radius: 16rpx;
  padding: 0 28rpx;
  font-size: 28rpx;
  color: #1f2937;
  box-sizing: border-box;
}

/* PDF上传区域 */
.upload-section {
  margin-bottom: 20rpx;
}

.upload-box {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40rpx 32rpx;
  border: 2rpx dashed #c7d2fe;
  border-radius: 16rpx;
  background: #eef2ff;
  transition: all 0.2s;
}

.upload-box:active {
  background: #e0e7ff;
}

.upload-box.uploading {
  border-color: #93c5fd;
  background: #eff6ff;
}

.upload-box.parsed {
  border-style: solid;
  border-color: #86efac;
  background: #f0fdf4;
}

.upload-icon {
  font-size: 48rpx;
  margin-bottom: 12rpx;
}

.upload-text {
  font-size: 28rpx;
  font-weight: 600;
  color: #2563eb;
  text-align: center;
  line-height: 1.4;
}

.upload-box.parsed .upload-text {
  color: #16a34a;
}

.upload-hint {
  margin-top: 8rpx;
  font-size: 24rpx;
  color: #9ca3af;
}

/* textarea 容器 */
.textarea-wrap {
  position: relative;
}

.textarea {
  height: 400rpx;
  background: #fff;
  border: 2rpx solid #e5e7eb;
  border-radius: 16rpx;
  padding: 28rpx;
  font-size: 28rpx;
  color: #1f2937;
  width: 100%;
  box-sizing: border-box;
  line-height: 1.6;
}

/* 一键粘贴按钮 */
.paste-btn {
  position: absolute;
  top: 16rpx;
  right: 16rpx;
  background: #f3f4f6;
  color: #2563eb;
  font-size: 24rpx;
  font-weight: 600;
  padding: 10rpx 20rpx;
  border-radius: 8rpx;
  z-index: 2;
}

/* 输入引导 */
.input-guide {
  margin-top: 12rpx;
  font-size: 24rpx;
  color: #9ca3af;
}

/* 字数统计行 */
.word-count-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 12rpx;
}

.privacy-tip {
  font-size: 22rpx;
  color: #6b7280;
}

.word-count {
  font-size: 22rpx;
  color: #d1d5db;
}

.word-count.warning {
  color: #ef4444;
}

.word-count.too-much {
  color: #f97316;
}

/* 信任文案 */
.trust-bar {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 36rpx 0 24rpx;
  font-size: 24rpx;
  color: #6b7280;
}

.trust-num {
  font-weight: 700;
  color: #2563eb;
  margin: 0 4rpx;
}

/* 主按钮 */
.submit-btn {
  margin-top: 48rpx;
  height: 100rpx;
  line-height: 100rpx;
  background: #f3f4f6;
  color: #9ca3af;
  font-size: 34rpx;
  font-weight: 700;
  border-radius: 50rpx;
  border: 2rpx solid #d1d5db;
  letter-spacing: 2rpx;
}

.submit-btn.active {
  background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
  color: #fff;
  border-color: transparent;
  box-shadow: 0 16rpx 40rpx rgba(37, 99, 235, 0.45);
}

.submit-btn::after {
  border: none;
}

/* 按钮辅助文案 */
.btn-hint {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 20rpx;
  font-size: 24rpx;
  color: #9ca3af;
}

/* 底部标签 */
.footer-tags {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 24rpx;
  margin-top: 56rpx;
  font-size: 24rpx;
  color: #d1d5db;
}

.tag-divider {
  color: #e5e7eb;
}

/* 数据看板入口 */
.admin-entry {
  margin-top: 32rpx;
  text-align: center;
  font-size: 24rpx;
  color: #ccc;
  padding: 16rpx;
}
